# 移动端界面验收

日期：2026-09-21

final result: passed

## 范围与设计依据

实现四个列表，以及详情的基本信息、折叠表单、流程进度、审批历史和 BPMN 查看器。按用户后续要求缩小待办/已办标题，并移除待办条目最前面的图标。上述差异是明确授权的设计调整。

参考图目录：`/Users/dawei/.codex/generated_images/01a0b8c1-562e-7fd3-a7ad-02644663ae11/`

| 页面 | 参考图 | 源像素尺寸 |
| --- | --- | --- |
| 待办 | exec-d39f888b-7e2a-4359-954e-9d974e540bbd.png | 853 × 1844 |
| 已办 | exec-095d1110-db9e-4334-a817-050d8bae5cdc.png | 853 × 1844 |
| 我发起的 | exec-3a20362b-f403-4b87-be67-cedaba6ec21e.png | 977 × 1610 |
| 知会 | exec-b0bb756d-f976-4559-ba7b-905f0be46bbc.png | 977 × 1610 |

运行地址：<http://localhost:3001/m/>。所有实现截图为已登录、浅色主题、390 × 844 CSS px，截图像素同尺寸，密度为 1。参考图按宽度等比缩放到 390 CSS px，不拉伸；前两张约 843 px 高，后两张约 643 px 高，因此后两页比较内容区和排版层级，不把画布高度差当成误差。统一使用 56 px 页头、44 px 搜索区和 64 px 底部导航。

原图为示意数据；实现使用当前真实列表，数量、标题、日期、状态与图中不同。知会截图中的记录均为已读，未为了截图修改业务状态。总数由服务端提供，不将当前页未读数量冒充全部未读数；排序说明不伪装成尚未实现的下拉菜单。

## 对照证据

证据目录：`/Users/dawei/Documents/ddup/ai/flow/.codex-artifacts/mobile-redesign/`（Git 忽略）。

- 原始实现：`todo-390.png`、`done-390.png`、`started-390.png`、`cc-390.png`。
- 同一画面内的左右对照：`comparison-todo.png`、`comparison-done.png`、`comparison-started.png`、`comparison-cc.png`，各为 820 × 900 px。对应 `compare-*.html` 可通过本地 Vite 查看。
- 字体、行距、状态、搜索和日期分组在上述 390 px 宽的并排对照中均可读；另逐页查看原始 390 × 844 实页截图，核查细节。无需额外局部裁切，未用缩略图替代字体和间距判断。
- 详情实页：`detail-basic-390.png`、`detail-progress-390.png`、`detail-history-390.png`、`detail-diagram-390.png`。详情没有另外生成设计图，依用户授权延续列表的白底、青绿色、细分隔线和紧凑字号。

## 发现、修复与复核

1. **P1，已修复：任务条目内容溢出。** 原 Inbox 的通用 `button` 规则将子组件根按钮限制为 44 px。工具栏改用专用选择器，条目高度随内容增长；三种宽度的长标题测试验证正文、页脚均落在条目内部。
2. **P2，已修复：已办/我发起的首屏密度不足。** 初次对照 `comparison-started-before.png` 中第五条被底栏遮挡。收紧已办垂直内边距、发起列表的元信息间距，最终 `comparison-started.png` 与 `comparison-done.png` 可完整展示五条当前长度的记录。长标题仍自然增高，不强行裁切。
3. **P2，已修复：知会长描述挤压流程名称。** 元信息改为自然行内排版，并优先展示已有 `nodeName`；知会人来自 `operatorName`，不将接收人当成发送人。最终 `cc-390.png` 无逐字挤压。
4. **P2，已修复：进度节点列表出现连线 ID。** 根据 BPMN 元数据过滤连线，图形画布保留完整连线和执行路径。`detail-progress-390.png` 只展示真实节点；自动化测试同时验证节点数量与实际 BPMN 缩放。
5. **验收修复：流程图按钮名称混入装饰图标字符。** 为图形操作补充明确的可访问名称，放大、适应屏幕和图入口均通过自动化验证。

## 五项视觉核查

- **字体：** 系统中文字体（PingFang SC 等回退），页头 20 px；待办/已办条目标题 15 px，其他列表 16 px；详情标题 18 px。标题按内容换行，姓名截断，正文与时间层级清楚。前两页标题小于原图符合用户要求。
- **间距：** 白底分隔行，左右 18 px 边距，统一搜索、分组、底栏对齐；不保留原来的大圆角卡片外框。360/390/430 宽度均无横向溢出，行高不限制长内容。
- **颜色：** 主色 `#196b62`、正文 `#18312e`、辅助 `#74817d`、分隔线 `#e8eeec`；待处理暖黄、通过青绿、驳回红、转办灰蓝，状态同时保留文字。BPMN 图使用同系状态色和图例。
- **图像与图标：** 无照片或装饰位图；使用项目现有 Vant 图标和真实 bpmn-js 矢量图。待办前置图标按最新指令移除，字母头像为界面控件。未引入新生成图片。
- **文案与数据：** 保留真实标题、办理结果、流程状态和时间；缺失结果显示“已办理”，不推断“已通过”；已结束实例不再显示“当前：-”。并行节点按真实状态分组，不虚构线性完成百分比。

## 功能与边界

- `npm run build:mobile`：通过，包括公共 packages 构建。
- 移动端浏览器回归：11 项全部通过。覆盖列表、折叠必填校验、子表、只读表单、扩展表单、审批竞争、下一办理人、驳回、转办、撤回、重新提交及流程图。写操作全部由测试接口拦截，未提交真实审批。
- 最后一次行距调整后，单独重跑三种宽度/四个列表的长标题布局回归：通过。
- 真实浏览器查看四个列表、基本信息、进度、历史和完整图；实际缩放 43% → 53% → 43%。最终稳定页面复核无新增控制台错误。构建期间出现的临时 HMR 模块重载/凭证错误未出现在构建完成后的稳定复核中。
- 本次样式限定在 `workflow-mobile` 和 `packages/workflow-mobile-ui`；PC 未导入移动组件库，未修改 PC 样式。
- 数据库迁移：本次无新增、修改或删除；Git 迁移差异检查为空。

## 剩余限制

浏览器视口和自动化触摸环境已验证，未做实体手机硬件测试。不同系统字体的字重会略有差异。没有待修复的 P0/P1/P2 视觉问题。
