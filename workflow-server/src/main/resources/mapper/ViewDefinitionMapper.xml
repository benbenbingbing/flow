<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.workflow.mapper.ViewDefinitionMapper">

    <!-- 分页查询视图列表 -->
    <select id="selectViewList" resultType="com.workflow.entity.ViewDefinition">
        SELECT * FROM view_definition
        WHERE status = 'ACTIVE'
        <if test="keyword != null and keyword != ''">
            AND (view_name LIKE CONCAT('%', #{keyword}, '%') OR view_code LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="viewType != null and viewType != ''">
            AND view_type = #{viewType}
        </if>
        <if test="entityCode != null and entityCode != ''">
            AND entity_code = #{entityCode}
        </if>
        ORDER BY created_at DESC
    </select>

</mapper>
