{
  "$schema": "../../../schemas/extension.schema.json",
  "$comment": "用途：日期格式。name=DateFormatter 是业务配置的稳定引用，移动文件不要改名。 在列表列的 renderComponent 中选择 name；该列 renderConfig 作为 config 传入。 实现：path 相对 workflow-web；default 是默认导出；kind=COMPONENT 表示 Vue 组件引用。 enabled 是构建安装开关，不代表数据库目录启停或页面已绑定。 configSchema 只定义参数，各实例的实际取值另存；每项 description 说明用途。",
  "schemaVersion": 1,
  "type": "LIST_CELL",
  "name": "DateFormatter",
  "label": "日期格式",
  "description": "按安全日期模板格式化，不执行表达式。",
  "version": 1,
  "implementation": {
    "path": "src/extensions/builtin/list-cells/DateFormatter.vue",
    "export": "default",
    "kind": "COMPONENT"
  },
  "metadata": {
    "configSchema": [
      {
        "key": "pattern",
        "label": "日期格式",
        "type": "text",
        "defaultValue": "yyyy-MM-dd HH:mm:ss",
        "description": "日期格式模板；当前实现支持 yyyy、MM、dd、HH、mm、ss 标记。"
      }
    ]
  }
}
