{
  "$schema": "../../../schemas/extension.schema.json",
  "$comment": "用途：状态标签。name=StatusBadge 是业务配置的稳定引用，移动文件不要改名。 在列表列的 renderComponent 中选择 name；该列 renderConfig 作为 config 传入。 实现：path 相对 workflow-web；default 是默认导出；kind=COMPONENT 表示 Vue 组件引用。 enabled 是构建安装开关，不代表数据库目录启停或页面已绑定。 configSchema 只定义参数，各实例的实际取值另存；每项 description 说明用途。",
  "schemaVersion": 1,
  "type": "LIST_CELL",
  "name": "StatusBadge",
  "label": "状态标签",
  "description": "使用标签展示状态，可配置标签尺寸和状态映射。",
  "version": 1,
  "implementation": {
    "path": "src/extensions/builtin/list-cells/StatusBadge.vue",
    "export": "default",
    "kind": "COMPONENT"
  },
  "metadata": {
    "configSchema": [
      {
        "key": "size",
        "label": "标签尺寸",
        "type": "select",
        "defaultValue": "small",
        "options": [
          {
            "label": "小",
            "value": "small"
          },
          {
            "label": "默认",
            "value": "default"
          },
          {
            "label": "大",
            "value": "large"
          }
        ],
        "description": "状态标签尺寸，可选值以 options 为准。"
      },
      {
        "key": "labelMap",
        "label": "文本映射",
        "type": "json",
        "jsonShape": "object",
        "helpKey": "entityList.statusLabelMap",
        "example": {
          "DRAFT": "草稿",
          "APPROVED": "已通过"
        },
        "description": "原始值到显示文本的精确映射。"
      },
      {
        "key": "statusMap",
        "label": "颜色映射",
        "type": "json",
        "jsonShape": "object",
        "helpKey": "entityList.statusColorMap",
        "example": {
          "draft": "info",
          "approved": "success"
        },
        "description": "小写状态值到标签类型的映射。"
      }
    ]
  }
}
