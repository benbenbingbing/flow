{
  "$schema": "../../../../schemas/extension.schema.json",
  "$comment": "用途：项目·后端 Schema 扩展列表。name=PROJECT_CUSTOM_LIST_SCHEMA 是业务配置的稳定引用，移动文件不要改名。 在实体列表中选择 name，实例参数由 viewConfig.customComponentProps 传入 config。 实现：path 相对 workflow-web；default 是默认导出；kind=COMPONENT 表示 Vue 组件引用。 enabled 是构建安装开关，不代表数据库目录启停或页面已绑定。 usageContexts=PAGE 用于普通页面；关联内容组件必须使用独立契约。 configSchema 只定义参数，各实例的实际取值另存；每项 description 说明用途。",
  "schemaVersion": 1,
  "type": "LIST",
  "name": "PROJECT_CUSTOM_LIST_SCHEMA",
  "label": "项目·后端 Schema 扩展列表",
  "description": "由后端 EntityListSchemaProvider 增强结构后使用项目看板渲染。",
  "version": 1,
  "enabled": true,
  "implementation": {
    "path": "src/extensions/business/project/lists/ProjectAcceptanceBoardList.vue",
    "export": "default",
    "kind": "COMPONENT"
  },
  "metadata": {
    "snapshotVersion": 1,
    "configSchema": [
      {
        "key": "showProviderTrace",
        "label": "显示 Provider 轨迹",
        "type": "boolean",
        "defaultValue": true,
        "description": "是否展示列表 Provider 的验收信息；不改变宿主的数据权限或查询范围。"
      },
      {
        "key": "searchPlaceholder",
        "label": "搜索提示",
        "type": "text",
        "defaultValue": "搜索 Schema 扩展列表",
        "description": "列表查询输入框的提示文案；实际查询仍使用宿主提供的查询能力。"
      }
    ],
    "capabilities": {
      "backendSchemaProvider": true,
      "reusesPlatformActions": true,
      "reusesPlatformPagination": true
    },
    "usageContexts": [
      "PAGE"
    ]
  }
}
