{
  "$schema": "../../../../schemas/extension.schema.json",
  "$comment": "用途：契约示例·文本长度。name=contract_text_length 是业务配置的稳定引用，移动文件不要改名。 在表单字段自定义校验中绑定 name/version；每个字段的规则 params 作为 validate(value, context) 的 context.params 传入。 实现：path 相对 workflow-web；createTextLengthValidator 是具名导出；kind=FACTORY 表示同步调用工厂创建校验器；工厂不可返回 Promise，validate 可以异步。 示例需同时满足 enabled=true 和 Demo 开关；开发模式默认打开 Demo，生产需 VITE_ENABLE_DEMO_EXTENSIONS=true。 supportedFieldTypes 声明兼容字段类型，设计时应按此范围选择。 configSchema 只定义参数，各实例的实际取值另存；每项 description 说明用途。",
  "schemaVersion": 1,
  "type": "VALIDATOR",
  "name": "contract_text_length",
  "label": "契约示例·文本长度",
  "version": 1,
  "enabled": false,
  "implementation": {
    "path": "src/extensions/examples/contracts/validatorFactory.js",
    "export": "createTextLengthValidator",
    "kind": "FACTORY"
  },
  "metadata": {
    "supportedFieldTypes": [
      "STRING",
      "TEXT"
    ],
    "configSchema": [
      {
        "key": "maxLength",
        "label": "最大字符数",
        "type": "number",
        "min": 1,
        "defaultValue": 50,
        "description": "按 Unicode 字符计数；每次调用读取当前绑定参数。"
      }
    ]
  }
}
