{
  "$schema": "../../../../schemas/extension.schema.json",
  "$comment": "用途：契约示例·整列表。name=ContractExampleList 是业务配置的稳定引用，移动文件不要改名。 在实体列表中选择 name，实例参数由 viewConfig.customComponentProps 传入 config。 实现：path 相对 workflow-web；default 是默认导出；kind=COMPONENT 表示 Vue 组件引用。 示例需同时满足 enabled=true 和 Demo 开关；开发模式默认打开 Demo，生产需 VITE_ENABLE_DEMO_EXTENSIONS=true。 usageContexts=PAGE 用于普通页面；关联内容组件必须使用独立契约。",
  "schemaVersion": 1,
  "type": "LIST",
  "name": "ContractExampleList",
  "label": "契约示例·整列表",
  "description": "",
  "version": 1,
  "enabled": false,
  "implementation": {
    "path": "src/extensions/templates/CustomListTemplate.vue",
    "export": "default",
    "kind": "COMPONENT"
  },
  "metadata": {
    "usageContexts": [
      "PAGE"
    ]
  }
}
