# Repository Instructions

## Flyway 迁移规范

- 已存在于主分支的 `V*__*.sql` 属于不可变迁移，禁止修改、删除或重命名。
- 新建表、字段、索引和约束时，只能新增更高版本号的迁移文件。
- 禁止为了维护完整数据库结构而同步修改 `V001__business_schema.sql`。
- 新数据库应按版本顺序执行全部迁移，不能依赖持续改写 V001 初始化。
- 当前分支新建且尚未合并的迁移文件可以继续修改；合并到主分支后即视为不可变。
- 创建迁移前必须先检查现有最大版本号，避免重复使用版本号。
- 需要建立全量基线时，应使用 Flyway `Bxxx__*.sql`，并在修改前获得用户明确确认。
- 不得使用 `flyway repair` 掩盖历史迁移文件变化；确需执行时必须先说明原因和数据库状态，并获得用户明确确认。
- 提交前必须检查 Git 差异，确保没有修改、删除或重命名主分支已有的版本迁移。
- 最终交付说明必须明确列出本次新增、修改或删除的迁移文件。

## 代码注释规范

- 新增或修改代码时，应为关键方法和复杂逻辑补充必要的注释。
- 对核心业务方法、公共接口以及行为不直观的方法添加方法级注释，说明：
  - 方法用途和业务含义；
  - 重要参数、返回值和异常；
  - 必要的前置条件、副作用或使用限制。
- 方法体中存在以下情况时，应在对应代码之前添加行内或块级注释：
  - 复杂条件分支、循环或算法；
  - 不直观的业务规则和特殊处理；
  - 状态流转、数据转换或多步骤处理；
  - 边界条件、兼容性处理和异常兜底；
  - 并发、事务、缓存、性能优化等容易产生误解的逻辑；
  - 为规避已知问题而采用的特殊实现。
- 注释应重点解释“为什么这样做”、业务约束和设计意图，不要只是重复代码表面行为。
- 简单、语义清晰的代码不强制添加注释，避免逐行注释、无意义注释和过度注释。
- 修改代码时必须同步检查并更新相关注释，禁止保留与实际行为不一致的过期注释。
- 注释语言和格式应遵循当前项目已有规范；没有明确规范时，业务说明优先使用中文，技术标识符保留原文。
- 提交前检查本次新增或修改的复杂逻辑是否具备足够注释，使不了解上下文的开发者能够理解其目的和约束。
