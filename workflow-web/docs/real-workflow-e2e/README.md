# 真实流程闭环证据

- `latest.json`：最近一次固定测试流程的完整配置与运行结果。
- `workflow-e2e-*.json`：每次真实流程闭环的结构化结果。
- `process-progress-final.png`：流程完成后的真实进度页截图。

固定测试流程为：

`提交申请 -> 配置校验审批 -> 财务复核审批 -> 审批完成`

验证内容包括节点名称、审批人、待办生成、中间状态、最终状态、流程进度、处理结果和审批意见。

执行命令：

```bash
npm run test:workflow:real
```
