{
  "$schema": "../../../../schemas/extension.schema.json",
  "$comment": "用途：项目·成员变更表单。name=ProjectMemberChangeForm 是业务配置的稳定引用，移动文件不要改名。 在实体表单中选择 name，实例参数由 viewConfig.customComponentProps 传入 config。 实现：path 相对 workflow-web；default 是默认导出；kind=COMPONENT 表示 Vue 组件引用。 enabled 是构建安装开关，不代表数据库目录启停或页面已绑定。 usageContexts=PAGE 用于普通页面；关联内容组件必须使用独立契约。 configSchema 只定义参数，各实例的实际取值另存；每项 description 说明用途。",
  "schemaVersion": 1,
  "type": "FORM",
  "name": "ProjectMemberChangeForm",
  "label": "项目·成员变更表单",
  "description": "支持加入、退出、暂停、恢复、投入调整及跨实体路由预判。",
  "version": 1,
  "enabled": true,
  "implementation": {
    "path": "src/extensions/business/project/forms/ProjectMemberChangeForm.vue",
    "export": "default",
    "kind": "COMPONENT"
  },
  "metadata": {
    "snapshotVersion": 1,
    "supportedModes": [
      "create",
      "edit",
      "approve",
      "view"
    ],
    "configSchema": [
      {
        "key": "title",
        "label": "表单标题",
        "type": "text",
        "defaultValue": "项目成员变更申请",
        "description": "当前组件实例显示的标题；只改变展示文案，不修改实体或表单的业务名称。"
      },
      {
        "key": "showRoutePreview",
        "label": "显示审批路径",
        "type": "boolean",
        "defaultValue": true,
        "description": "是否显示成员变更的审批路径预判；只是预览展示，实际路由由后端流程执行。"
      }
    ],
    "capabilities": {
      "exposesValidate": true,
      "computesWorkflowRouteFlags": true,
      "crossEntityContext": true
    },
    "usageContexts": [
      "PAGE"
    ]
  }
}
