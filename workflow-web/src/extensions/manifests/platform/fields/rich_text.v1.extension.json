{
  "$schema": "../../../schemas/extension.schema.json",
  "$comment": "用途：富文本。name=rich_text 是业务配置的稳定引用，移动文件不要改名。 在表单设计器的字段组件中选择 name；实例参数由 field.componentProps 传入。 实现：path 相对 workflow-web；default 是默认导出；kind=COMPONENT 表示 Vue 组件引用。 enabled 是构建安装开关，不代表数据库目录启停或页面已绑定。 supportedFieldTypes 声明兼容字段类型，设计时应按此范围选择。 defaultForFieldTypes 决定新增实体字段的默认控件；修改后运行 extensions:generate。 configSchema 只定义参数，各实例的实际取值另存；每项 description 说明用途。",
  "schemaVersion": 1,
  "type": "FIELD",
  "name": "rich_text",
  "label": "富文本",
  "version": 1,
  "implementation": {
    "path": "src/extensions/builtin/fields/components/RichTextField.vue",
    "export": "default",
    "kind": "COMPONENT"
  },
  "metadata": {
    "supportedFieldTypes": [
      "TEXT",
      "RICH_TEXT"
    ],
    "configSchema": [
      {
        "key": "height",
        "label": "编辑器高度",
        "type": "number",
        "min": 120,
        "max": 1000,
        "defaultValue": 200,
        "description": "富文本编辑区域高度，按控件支持的数值设置。"
      }
    ]
  },
  "aliases": [],
  "defaultForFieldTypes": [
    "RICH_TEXT"
  ]
}
