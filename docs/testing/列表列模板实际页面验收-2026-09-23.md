# 列表列模板实际页面验收（2026-09-23）

测试地址：`http://localhost:3000/config/list-column-templates`。测试数据是新建的 `CODEX_COLUMN_20260923` 与 `CODEX_COLUMN_20260923_COPY`，原有 `CODEX_STATUS_COLUMN` 未修改。

| 点位 | 实际页面步骤 | 结果 |
| --- | --- | --- |
| 新建必填 | 打开新建弹窗，空编码、空名称保存 | 表单显示必填校验，不产生模板 |
| 编码格式 | 在编码框输入小写字母与下划线 | 界面自动转成大写；创建后编码禁用，重新编辑不能改 |
| 数据来源 | 选择“字段组合模板”，输入 `${code}: ${name}` | 保存后重新打开，来源仍为字段组合模板且表达式回显 |
| 查询配置 | 开启“作为查询条件”，选择“等于”，输入占位提示“输入编码或名称” | 保存、重新打开后开关、方式、提示均回显；列表摘要显示“查询 EQ” |
| 显示配置 | 设置显示、宽度 180、居中、最小宽度 120、溢出提示、快捷复制、默认文本渲染及空值文本“无值” | 保存、重新打开后各控件回显；摘要显示“180px · 居中 · 查询 EQ · 快捷复制 · DefaultText · FIELD_TEMPLATE” |
| 单元格预览 | 将示例原始值输入“样例数据” | 预览区域同步显示“样例数据”。预览仅证明渲染外观，不代表实际列表数据源运行正确 |
| 模板复制 | 点击原模板“复制”，保存自动预填 `_COPY` 编码和“副本”名称 | 列表出现独立副本；两份初始摘要一致 |
| 复制独立性 | 将原模板宽度从 180 改为 190，保存并刷新列表 | 原模板摘要变为 190px，副本仍为 180px；模板间后续修改不串联 |
| 副本来源与查询 | 编辑副本，来源改为“实体字段”，查询方式改为“包含”，控件选“文本输入”，提示改为“输入值” | 保存后副本摘要显示“查询 LIKE”，不再显示 `FIELD_TEMPLATE`；原模板仍显示“查询 EQ · FIELD_TEMPLATE” |
| 来源条件联动 | 暂选“项目自定义日志字段” | 仅显示“展示前缀”，查询开关自动关闭且查询方式、控件、默认值禁用；取消编辑后不污染已保存配置 |
| 查询选项呈现 | 打开查询方式与查询控件下拉 | 页面列出等于、不等于、包含、不包含、大于、大于等于、小于、小于等于，以及文本、数字、单双选、日期、日期时间、用户、部门控件；仅验证选项可见，未逐一验证运行行为 |
| 搜索 | 输入“Codex列表列验收” | 列表只显示上述原模板和副本 |
| 应用到实体列 | 在 `codex_accept_20260923_main` 新建虚拟列 `codex_code_name`，在“数据与显示→模板初始化”选择原模板 | 组合表达式变为 `${code}: ${name}`，空值文本变为“无值”，查询 EQ、宽度 190、居中和快捷复制均复制到当前列；发布后列状态与发布版本一致 |
| 真实列表渲染 | 打开列表设计的“预览”，它通过列表运行时接口读取 3 条实际记录 | 显示 `CODEX_ACCEP_6AD9D66B20260923000004: QA查询普通值` 等三条 `code: name` 组合，而非仅显示示例占位 |
| 真实列表查询 | 展开查询区，用一条完整组合值做等于查询，再输入不存在的组合值，最后重置 | 匹配时 3→1 条，未匹配时 0 条，重置后恢复 3 条；虚拟列查询条件实际生效 |
| 模板应用后独立性 | 列表已发布后把源模板宽度从 190 改成 200，再重新打开列表设计 | 源模板摘要为 200px；已应用虚拟列仍为 190px，列表仍显示“已与发布版本一致”，不会偷偷变更运行列 |
| 平板预览布局 | 切换“平板”，展开查询区查看四条真实行 | 查询栏在窄容器内正常换行，组合列按配置宽度截断并保留复制按钮，表头、分页和关闭按钮均可见；查询输入显示配置的占位提示 |

这次验证了模板配置保存、回显、复制独立性，以及字段组合模板在当前实体列表的真实数据渲染和等于查询。尚未逐个验证其他来源、查询操作符、渲染组件，以及其他用户权限范围下的表现。

操作便捷性建议：复制模板后，弹窗默认落在“基本信息”较易核对新编码；但目前列表仅在摘要里显示部分配置，若要比对两个模板的数据来源参数和查询控件，需分别进入编辑页。可考虑只读详情或差异预览。此为建议，未改代码。

本子任务新增、修改、删除的 Flyway 迁移文件：无。
