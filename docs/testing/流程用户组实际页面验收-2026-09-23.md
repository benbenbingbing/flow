# 流程用户组实际页面验收（2026-09-23）

测试地址 `http://localhost:3000/config/process-user-groups`；只新增专用组 `codex_accept_group_20260923`，没有修改现有四个用户组。组目前启用、无成员，留作后续按用户组选人测试。

| 点位 | 实际页面步骤 | 结果 |
| --- | --- | --- |
| 页面布局 | 1280px 宽度查看原有 4 组 | 原界面固定操作列盖住状态开关，创建时间完全不可见；已调整列宽并将时间显示为 `YYYY-MM-DD HH:mm:ss`，截图复测所有列完整可见 |
| 新增必填 | 空组名称、编码提交 | 同时显示“请输入组名称”“请输入组编码”，弹窗保留 |
| 新增配置 | 填名称、编码、描述、排序 10、启用并创建 | 列表新增第 5 行；刷新后名称、编码、描述、排序、启用均回显 |
| 编码唯一性 | 用另一个名称提交相同编码 | 服务端拒绝；原页面仅留下弹窗并在控制台出现未处理异常、没有可见反馈。已在弹窗中增加持久错误提示，复测显示“组编码已存在：codex_accept_group_20260923”，未新增重复组 |
| 编辑配置 | 修改描述、排序为 11 | 列表立即更新；再次打开编辑，编码为禁用状态且值不变 |
| 状态切换 | 禁用专用组，再启用 | 两次均显示影响成员数和历史任务不会改派的确认；确认后状态分别变“禁”“启” |
| 成员选择 | 在 35 个候选用户中选 admin，确认并保存 | 变更摘要显示新增 1、总数 0→1；保存成功，刷新后成员数 1，重开弹窗保留 admin 标签 |
| 成员无变化 | 不更改成员再保存 | 提示“成员未发生变化”，没有重复提交 |
| 成员移除 | 移除 admin 标签并保存 | 摘要显示移除 1、总数 1→0；保存后列表成员数恢复 0 |
| 删除保护 | 点击专用组“删除” | 提示输入完整组名，说明删除后成员将失去流程候选资格；取消后组保留。未提交不可恢复删除 |

后续仍需把此组接入独立测试流程的用户任务，验证组成员增减对任务候选资格的运行影响；本轮仅验证用户组配置与持久化。

本子任务新增、修改、删除的 Flyway 迁移文件：无。
