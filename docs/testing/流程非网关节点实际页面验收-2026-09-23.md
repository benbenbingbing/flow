# 流程非网关节点实际页面验收（2026-09-23）

测试入口：`http://localhost:3000/process/design/2102668009519804417`，专用流程“Codex网关逐项验收20260923”。本记录只陈述已在真实页面操作并观察到的结果；未发布、未运行的配置不计为逻辑通过。

| 配置点 | 页面操作与实际结果 | 结论 |
| --- | --- | --- |
| 用户任务节点 | 从画布工具栏拖入任务，变更为“用户任务”，右侧出现常用、协同、高级、流程动作四个页签。节点名称可编辑。 | 页面配置可操作，运行待测 |
| 执行人指定 | 指定方式下拉包含固定人员、用户组、角色、实体用户关系字段、相对组织职务、使用其他节点审批人、表达式、接口动态。选择用户组后，可选到专用组 `codex_accept_group_20260923`，并有组内人员展开说明。 | 配置选项和组数据回显通过，任务分配运行待测 |
| 多人办理 | 开启后出现会签/或签、并行/串行多实例。会签显示通过率阈值与“是否需要所有人审批”；切换或签后这两项消失。或签与串行可选择。 | 页面联动通过，运行待测 |
| 下一节点审批人 | 开启“审批时展示”后新增“允许修改”及人员来源；开启修改后状态文案为“展示并可修改”。人员来源可切换本节点审批人、人员范围、人员接口。人员范围支持指定用户、组织部门、角色、用户组、全部启用用户；人员接口提供受控解析器及 `extraParams`，页面明确不接受任意 URL。 | 页面联动通过，人员解析运行待测 |
| 办理表单与审批 | 页面展示实体默认表单、强制整表只读、审批意见及“通过/驳回”动作的名称、编码、样式和备注控制。 | 已检查控件，未逐项保存及运行 |
| 知会 | 开启后可同时选择“任务创建时”“任务完成时”，站内渠道、本人接收、手工添加及收件人规则可见。收件人规则下拉有固定用户、角色成员、用户组成员、组织/部门成员、流程发起人、当前办理人、历史办理人、实体字段用户、受控解析器。选择两种触发时机后，说明文案同步包含两者。 | 页面联动通过，异步通知运行待测 |
| 自动跳过 | 支持不跳过、始终跳过、满足条件时跳过。条件模式提供 AND/OR、条件/条件组；选择金额、大于、1000 后，只读完整表达式显示 `${amount > 1000}`。 | 表达式生成通过，运行待测 |
| 任务 SLA | 开启后可选择已发布策略；选择“1分钟测试（v1）”后摘要同步显示。日历来源包含节点指定、流程指定、业务归属部门、发起人部门、系统默认；节点指定出现必填节点日历。 | 页面联动通过，截止时间与升级运行待测 |
| 节点动作 | 添加弹窗的“审批前校验”模板自动填入动作名“审批前业务校验”和“任务提交、流程流转前”。触发时机菜单包含进入节点、节点完成/路由计算前、待办创建后、办理人分配或变更后、任务提交/流程流转前。事务内/提交后切换后，失败策略从回滚/继续转为自动重试，出现最大重试 5 和死信说明。处理器下拉展示当前可用动作。最终取消，未创建动作。 | 配置联动通过，处理器执行运行待测 |
| 草稿保存与发布预检 | 未配置执行人的独立任务节点“应用到画布”后保存草稿成功，刷新仍可见。流程列表点击“发布”出现 5 个阻断：该节点不可达、没有办理人来源、默认空办理人策略不能解析，以及已有两条排他网关非默认连线缺少表达式；发布按钮置灰。随后在设计器删除临时任务节点并再次保存草稿。 | 草稿持久化和发布阻断通过 |

除最后一行的无办理人负例外，其他配置更改未“应用到画布”，不可将页面联动观察当作配置保存回显或运行逻辑通过。后续需建立独立可发布流程，逐项验证任务分配、多实例、知会、跳过、SLA 和流程动作实际执行。

操作便捷性建议：同一节点未配置办理人时，发布预检同时显示 `EMPTY_ASSIGNEE_POLICY_INVALID` 和“用户任务没有配置办理人来源”，对用户而言重复；建议合并为一条可定位到节点与字段的提示。
