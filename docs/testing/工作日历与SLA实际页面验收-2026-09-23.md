# 工作日历与 SLA 实际页面验收（2026-09-23）

入口：`http://localhost:3000/system/work-calendars`，独立测试日历 `CODEX_CAL_20260923`，没有设为系统默认，也没有绑定真实组织或部门。

| 点位 | 实际页面操作与结果 | 结论 |
| --- | --- | --- |
| 新建基本信息 | 新建弹窗包含编码、名称、IANA 时区、默认标记、生效日期、说明。空编码和名称点保存时弹窗保持打开；填写专用编码与名称、Asia/Shanghai 后保存，列表新增草稿，显示非默认、版本 1。 | 创建与必填阻断通过 |
| 工作时段 | 新建时预置周一至周五 09:00–12:00、13:00–18:00 两段，页面说明可添加多班次。重新打开后十条时段完整回显。 | 保存回显通过；重叠与边界校验待测 |
| 特殊日期 | 新增 2026-09-25“休息日 / Codex测试休息日”，保存并重开后日期、类型、名称回显。 | 保存回显通过；实际时间计算待测 |
| 作用域绑定 | 新增行默认“部门”，下拉还有“组织”，并显示范围 ID、优先级、生效开始与结束。测试未填写范围，随后移除该行并保存。 | 页面选项通过；实际作用域选择待测 |
| 发布 | 列表点击专用日历的“发布”，确认后状态从草稿变为已发布，按钮变为“停用”，页面提示“工作日历已发布”。 | 发布通过；被节点引用后的截止时间待测 |
| 节点 SLA | 流程设计器的用户任务开启 SLA 可选已发布“1分钟测试（v1）”，日历来源包含系统默认、节点指定、流程指定、业务归属部门、发起人部门；节点指定出现必填节点日历。 | 页面联动通过；发布快照及实际截止时间待测 |
| SLA 策略基本信息 | `/system/sla/policies` 新建独立策略 `CODEX_SLA_20260923`，响应 5 分钟且为自然时间、办结 15 分钟且为工作时间；保存后列表显示版本 1 草稿，重开配置数值和计时方式完整回显。 | 保存回显通过 |
| 提醒与升级步骤 | 新增“到期提醒验收”，指标首次响应、到期前 1 分钟、动作提醒当前人、次数 1；保存重开后名称、指标、时机、偏移、动作、次数及站内收件人 JSON 回显。动作下拉还包含提醒上级、增加知会、自动转办、自动加签。 | 保存回显通过；实际提醒触发待测 |
| SLA 发布 | 独立策略发布确认后，列表状态由草稿变已发布，并提示“SLA策略已发布”。 | 发布通过；被流程版本引用后的截止时间待测 |
| SLA 监控 | `/system/sla/monitor` 初始卡片显示运行中 0、已超时 8、已暂停 0、已完成 11；列表共 19 条历史任务。选择“已超时”查询仅剩 8 条，重置后恢复 19 条。 | 页面筛选通过；本轮新策略尚无运行任务 |

便捷性建议：新建日历弹窗较长，保存按钮固定在底部；空必填项提交后保持弹窗，但页面未滚到错误字段，当前视口难以发现缺哪一项。建议保存失败时聚焦并滚动到首个错误字段。

SLA 步骤表宽于当前弹窗，右侧“接收人配置”需要横向滚动，且“动作配置/接收人配置”直接显示 JSON。建议对常见提醒动作提供结构化字段，再给高级用户保留 JSON 编辑。
