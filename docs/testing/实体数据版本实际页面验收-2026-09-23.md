# 实体数据版本实际页面验收（2026-09-23）

本轮按用户要求在当前用例结束后停止新增测试。使用 `http://localhost:3000` 管理员实际页面，创建并发布独立实体 `codex_version_20260923`，其默认列表 `codex_version_20260923_default` 和默认表单均已发布。测试记录 ID 为 `7e1f12b05d024f4399b33153871a359c`。

| 点位 | 实际操作与结果 | 判断 |
| --- | --- | --- |
| 策略启用和范围 | 页面启用数据版本；根实体从“全部已发布字段”切到“指定字段”，选择 `name`、`note`；结构预览显示“结构有效”。保存时出现“当前只固化根实体，未选择任何组成关系”提示，点击“继续保存”后列表显示“已启用”、4 条生成时机；关闭重开后两个字段仍选中 | 页面配置、保存生效通过；关联范围未测 |
| 版本保护上限 | 将单关系最多、整版最多、整版大小分别设置为 3 行、10 行、1 MiB 并提交，关闭重开仍分别回显 3/10/1 | 页面输入与保存回显通过；超限运行未测 |
| 比对规则 | 保留“打开时仅看变化”，开启“追踪关联行顺序”，选择忽略 `note`；关闭重开仍为上述值 | 页面配置与保存回显通过；无关联行，顺序变化运行未测 |
| 新增生成时机 | 新增根实体变化触发器 `ROOT_WRITE`，名称“普通写入固化”、全部入口、全部操作、条件 `{}`、优先级 50、启用；与默认三条生成时机同时保存 | 列表回显 4 条；下面的运行写入实际由该触发器命中 |
| 新增记录 | 真实列表的“新增数据”输入 `name=VERSION_A`、`note=初始备注` 并保存 | 列表 1 条；版本抽屉出现 V1“普通写入固化”，来源“表单”、操作人 admin |
| 只改忽略字段 | 编辑 `note=仅备注改变`，名称不变 | 版本抽屉出现 V2；V2 表单快照包含新备注，但 V1→V2 比较不显示 `note` 变化，只显示系统 `update_time` 变化，符合忽略字段规则 |
| 改参与比较字段 | 编辑 `name=VERSION_B`，备注不变 | 版本抽屉出现 V3；V2→V3 比较显示业务字段“数据名称”从 VERSION_A 到 VERSION_B，另有 `update_time` 变化 |

本次没有测试手工固化、条件组合、关联数据变化、关联范围过滤、容量上限拒绝、关联行排序比对等点位；这些仍保留在 625 项逐项清单中为“待验证”。

易用性建议：只修改被忽略字段时，差异页仍以系统 `update_time` 作为唯一变化，容易让用户误以为业务内容发生变化。可考虑让系统时间字段默认不进入业务差异，或把它与业务差异分开显示。本轮仅记录建议，没有改动产品行为。

测试配置：专用实体、列表、表单和数据版本策略保留，便于复核；未修改业务既有实体。
