{
  "name": "flow-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "workflow-web",
    "workflow-mobile",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=10"
  },
  "scripts": {
    "build:packages": "npm run extensions:generate --workspace workflow-web && npm run build --workspace @flow/workflow-core && npm run build --workspace @flow/workflow-api && npm run build --workspace @flow/workflow-mobile-ui --if-present",
    "build:web": "npm run build:packages && npm run build --workspace workflow-web",
    "build:mobile": "npm run build:packages && npm run build --workspace workflow-mobile",
    "build": "npm run build:packages && npm run build --workspace workflow-web && npm run build --workspace workflow-mobile",
    "test:web": "npm run build:packages && npm run test --workspace workflow-web",
    "test:packages": "node --test packages/*/tests/*.test.mjs",
    "test:mobile": "npm run test --workspace workflow-mobile",
    "dev:web": "npm run dev --workspace workflow-web",
    "dev:mobile": "npm run dev --workspace workflow-mobile"
  },
  "overrides": {
    "vue": "3.5.29"
  }
}
